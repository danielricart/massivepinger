replicaCount: 1

image:
  repository: ghcr.io/example/massivepinger
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 9123

# List of ping targets rendered into the ConfigMap as config.yaml.
# Example:
# config:
#   - target: 8.8.8.8
#     interval: 5s
#     timeout: 1s
#   - target: 1.1.1.1
#     interval: 10s
#     timeout: infinite
config: []

serviceMonitor:
  # Enable to create a Prometheus Operator ServiceMonitor for scraping metrics.
  # Requires the prometheus-operator CRDs to be installed in the cluster.
  enabled: false
  # namespace: ""  # defaults to release namespace
  interval: 30s
  scrapeTimeout: 10s
  # Additional labels to add to the ServiceMonitor (e.g. to match a Prometheus
  # instance's serviceMonitorSelector).
  labels: {}

resources: {}

nodeSelector: {}

tolerations: []

affinity: {}
