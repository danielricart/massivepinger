apiVersion: v1
kind: Service
metadata:
  name: {{ include "massivepinger.fullname" . }}
  labels:
    {{- include "massivepinger.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "massivepinger.selectorLabels" . | nindent 4 }}
  ports:
    - name: metrics
      port: {{ .Values.service.port }}
      targetPort: metrics
      protocol: TCP
